'use strict';

describe('myPortfolio.project module', function() {

  beforeEach(module('myPortfolio.project'));

  describe('project controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ProjectCtrl');
      expect(ProjectCtrl).toBeDefined();
    }));

  });
});